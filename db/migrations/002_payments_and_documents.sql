-- ============================================================
-- Migration 002: OBSOLETE
-- Originally created payments / participant_documents tables for
-- the old inquiry/quotation/booking model. All of that schema is
-- dropped and replaced by migration 003 (PRD v1.5 refactor).
--
-- Kept as a no-op so historical migration sequence stays intact.
-- ============================================================

-- intentionally empty
SELECT 1;
